export const suma = (a, b) => {
	const result = `suma: ${parseInt(a)+parseInt(b)}`
	console.log(result);
  return result;
}
export const resta = (a, b) => {
	const result = `resta: ${parseInt(a)-parseInt(b)}`
	console.log(result);
  return result;
}
export const multiplicacion = (a, b) => {
	const result = `multiplicación: ${parseInt(a)*parseInt(b)}`
	console.log(result);
  return result;
}
export const division = (a, b) => {
	const result = `división: ${parseInt(a)/parseInt(b)}`
	console.log(result);
  return result;
}